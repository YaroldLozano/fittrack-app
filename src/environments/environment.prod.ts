// Reemplaza apiUrl por la URL pública real de fitness-api antes de publicar un
// build de producción — 'localhost' solo funciona para correr todo en la misma
// máquina (demo/evaluación local vía XAMPP), no en un despliegue real.
export const environment = {
  production: true,
  apiUrl: 'http://localhost/fitness-api'
};
